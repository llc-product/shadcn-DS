node_modules
dist
.next
out
coverage
*.tsbuildinfo
.DS_Store

# Written by CI at publish time from CI_JOB_TOKEN — never committed (see .npmrc.example).
.npmrc

# Raw Figma MCP dumps (token sync pipeline).
.figma-cache
