// The jsdom stubs live in @digitaltwin/testing/setup so every React suite in this org gets the
// same ones. This file exists to give vitest a path inside the package, and as the place to add a
// stub that only this package needs.
import "@digitaltwin/testing/setup";
